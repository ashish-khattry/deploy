//02_heap_clone_machine
fn main() {
    let original_data = String::from("This is original data!");
    let clone_data = original_data.clone();
    println!("Original data={original_data} and clone data={clone_data}");
}
